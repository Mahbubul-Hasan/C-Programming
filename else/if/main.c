#include <stdio.h>
#include <stdlib.h>

int main()
{   int a;
    printf("Enter the value \n");
    scanf("%d",&a);
    if (a>0)
        printf("This is positive \n");
    if (a<0)
        printf("This is negative \n");
    return 0;
}
// Write a program that tells whether an integral taken from keyboard is positive or negative
