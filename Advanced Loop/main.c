#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,sum;
    printf("Enter two numbers\n");
    scanf("%d%d",&a,&b);
    while (1)
    {
        sum=a+b;
    }
     printf("The sum is %d",sum);
    return 0;
}
