#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,x;
    printf("Enter the values \n");
    scanf("%d%d",&a,&b);
    if (x==1)
    printf("The sum is %d+%d=%d \n",a,b,a+b);
    if (x==2)
    printf("The product is %d*%d=%d \n",a,b,a*b);
    return 0;
}
